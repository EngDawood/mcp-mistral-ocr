name = "mcp-mistral-ocr"
main = "src/worker.ts"
compatibility_date = "2025-01-28"
compatibility_flags = ["nodejs_compat"]

# Node.js compatibility is required for:
# - Buffer operations
# - Blob API
# - @mistralai/mistralai SDK

[env.production]
name = "mcp-mistral-ocr"

[env.staging]
name = "mcp-mistral-ocr-staging"

# Environment variables (public, non-sensitive)
# For sensitive values like API keys, use Cloudflare dashboard or:
#   wrangler secret put MISTRAL_API_KEY

# [vars]
# PUBLIC_VAR = "value"
